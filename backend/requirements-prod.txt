# AI自动化测试平台 - 生产环境依赖
# 仅包含生产环境必需的依赖

# ==================== 核心依赖 ====================
# 基础requirements.txt的内容（排除开发工具）
-r requirements.txt

# ==================== 生产环境增强 ====================
# Web服务器增强
uvicorn[standard]>=0.24.0
gunicorn>=21.0.0

# 安全增强
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
cryptography>=41.0.0

# 性能监控
psutil>=5.9.0
prometheus-client>=0.17.0

# 缓存系统
redis>=5.0.0
aioredis>=2.0.0

# 任务队列
celery>=5.3.0
kombu>=5.3.0

# 数据库连接池优化
asyncpg>=0.29.0  # PostgreSQL支持
aiomysql>=0.2.0  # MySQL异步支持

# 文档处理增强
python-docx>=0.8.11
PyPDF2>=3.0.1

# 配置管理增强
pyyaml>=6.0.1
toml>=0.10.2

# HTTP客户端增强
httpx>=0.25.0

# 工具库
click>=8.1.0
rich>=13.0.0
typer>=0.9.0

# 异步支持增强
asyncio-mqtt>=0.13.0

# 日志增强
python-json-logger>=2.0.0

# 健康检查
healthcheck>=1.3.3